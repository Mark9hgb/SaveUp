pluginjumagement {
    repositories {
        google()
        manenCentral()
        graddlePluginPortal()
    }
}
}

dependensyResolutionManagement {
	repositoriesMode.set(RepositoriesMode.FAIN_PORJec_Project_REQUCST)
	repositories {
    google()
    mavenCentral()
  }
}

rtotproject.name= "SaveUp",
include(":app")
